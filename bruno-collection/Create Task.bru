meta {
  name: Create Task
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/tasks
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "New Task from Bruno",
    "description": "Testing POST request",
    "priority": "high"
  }
}

docs {
  Create a new task. Required: title. Optional: description, priority (high/medium/low)
}
